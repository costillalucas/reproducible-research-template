# ptyco-full-simulator

TODO: replace this with your real deliverable. Every number you quote here
must come from `data/numbers.json` (written *only* by
`scripts/compute_numbers.py`) via one of two tags —
`scripts/check_provenance.py` refuses to pass otherwise. Full syntax is in
the repo README's "The mechanism" section: an existence tag backing a
statement without a literal number, and a numeric tag backing a literal
value, checked to half a unit in its own last displayed digit. See them
used for real just below.

Placeholder, so the pipeline is runnable end to end from the first commit:
the provenance mechanism is wired up `[srcnum:template_wired:1]`
[src:template_wired].

See `examples/pi_estimator/report.md` for a fuller worked example, and the
repo README's "Adapting this to a real project" section for the checklist.
